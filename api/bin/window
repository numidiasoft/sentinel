#!/bin/bash
docker rm console 2> /dev/null
docker run --rm -ti --name window -v  /home/core/share/sentinel/api/:/app -v /srv/.sentinel-api:/.bundle --link mongodb:mongodb numidiasoft/sentinel-api /bin/bash
